#include <stdio.h>
#include <stdlib.h>

int main()
{
    int day;
    int first;
    scanf("%d", &day);
    scanf("%d", &first);

    for (int month = 1; month <= 12; month++) {
        int date;
        if (month == 1 || month == 3 || month == 5)
    }

    return 0;
}
