#include <stdio.h>
#include <stdlib.h>

int main()
{
    int barber;
    int customer;
    scanf("%d %d", &barber, &customer);

    int speed[barber];

    for (int i = 0; i < barber; i++) {
        scanf("%d", &speed[i]);
    }

    for (int i = 0; i < customer; i++) {

    }
}
