#include <stdio.h>
#include <stdlib.h>
#include <string.h>

main()
{
    char a[1000];
    char b[1000];
    scanf("%s %s", &a, &b);
    int length_a = strlen(a);
    int length_b = strlen(b);
    for(i=0; i<strlen(a); i++) {
        if
    }
}
